/**
 * < Return a negative version of a number >
 * @param {number} n - input number
 * @returns {number} negative version of `n`
 */

function asNegative(n) {
  if (n > 0) {
    return -n
  } else {
    return n
  }
}

console.log(asNegative(5));
console.log(asNegative(5));
